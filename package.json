{
  "name": "bookings-dashboard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node back-end/server.js",
    "client": "npm --prefix front-end run dev",
    "build": "npm --prefix front-end run build",
    "preview": "npm --prefix front-end run preview",
    "dev": "concurrently \"npm start\" \"npm run client\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "concurrently": "^9.2.1"
  },
  "devDependencies": {}
}
